name: Validate and apply render CI fixes

on:
  pull_request:
    branches:
      - codex/m3-interactive-controls-text-opengl

permissions:
  contents: write

jobs:
  clang:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: agent/render-backend-refactor
      - name: Apply fixes
        shell: bash
        run: |
          set -euo pipefail
          tr -d '\r' < .github/refactor-fix/ci-fixes.patch > /tmp/ci-fixes.patch
          echo "f66141f2af036684faaba06ea7f393d5a7e411572f986275f99f2e7bf9627b0d  /tmp/ci-fixes.patch" | sha256sum -c -
          git apply --check --whitespace=error /tmp/ci-fixes.patch
          git apply --whitespace=error /tmp/ci-fixes.patch
      - name: Configure Clang sanitizer build
        shell: bash
        run: |
          CC=clang CXX=clang++ cmake -S . -B out/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            "-DCMAKE_C_FLAGS=-fsanitize=address,undefined -fno-omit-frame-pointer" \
            "-DCMAKE_CXX_FLAGS=-fsanitize=address,undefined -fno-omit-frame-pointer" \
            "-DCMAKE_EXE_LINKER_FLAGS=-fsanitize=address,undefined" \
            -DAERO_BUILD_SHARED=OFF \
            -DAERO_BUILD_TESTS=ON \
            -DAERO_ENABLE_EXCEPTIONS=OFF \
            -DAERO_ENABLE_RTTI=OFF \
            -DAERO_WARNINGS_AS_ERRORS=ON \
            -DAERO_ENABLE_D3D11_BACKEND=OFF
      - name: Build and test
        run: |
          cmake --build out/build --parallel 2
          ctest --test-dir out/build --output-on-failure

  windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - build_type: Debug
            shared: OFF
          - build_type: Release
            shared: ON
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: agent/render-backend-refactor
      - name: Apply fixes
        shell: bash
        run: |
          set -euo pipefail
          tr -d '\r' < .github/refactor-fix/ci-fixes.patch > /tmp/ci-fixes.patch
          echo "f66141f2af036684faaba06ea7f393d5a7e411572f986275f99f2e7bf9627b0d  /tmp/ci-fixes.patch" | sha256sum -c -
          git apply --check --whitespace=error /tmp/ci-fixes.patch
          git apply --whitespace=error /tmp/ci-fixes.patch
      - name: Configure
        run: cmake -S . -B out/build -A x64 -DAERO_BUILD_SHARED=${{ matrix.shared }} -DAERO_BUILD_TESTS=ON -DAERO_ENABLE_EXCEPTIONS=OFF -DAERO_ENABLE_RTTI=OFF -DAERO_WARNINGS_AS_ERRORS=ON -DAERO_ENABLE_D3D11_BACKEND=ON
      - name: Build
        shell: pwsh
        run: |
          cmd /c "cmake --build out/build --config ${{ matrix.build_type }} --parallel > out\msvc-${{ matrix.build_type }}-build.log 2>&1"
          $buildExitCode = $LASTEXITCODE
          Get-Content out/msvc-${{ matrix.build_type }}-build.log
          exit $buildExitCode
      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: refactor-msvc-${{ matrix.build_type }}-build-log
          path: out/msvc-${{ matrix.build_type }}-build.log
          if-no-files-found: error
          retention-days: 7
      - name: Test
        shell: pwsh
        run: |
          cmd /c "ctest --test-dir out/build -C ${{ matrix.build_type }} --output-on-failure > out\msvc-${{ matrix.build_type }}-test.log 2>&1"
          $testExitCode = $LASTEXITCODE
          Get-Content out/msvc-${{ matrix.build_type }}-test.log
          if ($testExitCode -ne 0) { exit $testExitCode }
          cmd /c "ctest --test-dir out/build -C ${{ matrix.build_type }} -R AeroD3D11.*Tests -V > out\msvc-${{ matrix.build_type }}-warp.log 2>&1"
          $warpExitCode = $LASTEXITCODE
          Get-Content out/msvc-${{ matrix.build_type }}-warp.log
          exit $warpExitCode
      - name: Upload test logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: refactor-msvc-${{ matrix.build_type }}-test-logs
          path: |
            out/msvc-${{ matrix.build_type }}-test.log
            out/msvc-${{ matrix.build_type }}-warp.log
          if-no-files-found: ignore
          retention-days: 7

  commit:
    needs: [clang, windows]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: agent/render-backend-refactor
          fetch-depth: 0
      - name: Apply and commit fixes
        shell: bash
        run: |
          set -euo pipefail
          tr -d '\r' < .github/refactor-fix/ci-fixes.patch > /tmp/ci-fixes.patch
          echo "f66141f2af036684faaba06ea7f393d5a7e411572f986275f99f2e7bf9627b0d  /tmp/ci-fixes.patch" | sha256sum -c -
          git apply --index --whitespace=error /tmp/ci-fixes.patch
          rm -rf .github/refactor-fix
          rm -f .github/workflows/apply-refactor-ci-fixes.yml
          git add -A
          git config user.name "aerogui-refactor-bot"
          git config user.email "actions@users.noreply.github.com"
          git commit -m "Fix cross-platform render backend build"
          git push origin HEAD:agent/render-backend-refactor
