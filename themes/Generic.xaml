<ResourceDictionary
    xmlns="urn:aero"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource ControlForeground}"/>
  </Style>

  <Style TargetType="Button">
    <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
    <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8"/>
    <Setter Property="Foreground" Value="{StaticResource ControlForeground}"/>
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="Button">
    <ControlTemplate.VisualTree>
      <Grid>
        <Border x:Name="Chrome"
                Background="{StaticResource ControlBackground}"
                BorderBrush="{StaticResource ControlBorder}"
                BorderThickness="1"
                Padding="8">
          <ContentPresenter x:Name="ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
        </Border>
        <Border x:Name="FocusCue"
                BorderBrush="#00000000"
                BorderThickness="1"/>
      </Grid>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualStateGroup.Transitions>
          <VisualTransition GeneratedDuration="0:0:0.09">
            <VisualTransition.GeneratedEasingFunction>
              <QuadraticEase EasingMode="EaseOut"/>
            </VisualTransition.GeneratedEasingFunction>
          </VisualTransition>
        </VisualStateGroup.Transitions>
        <VisualState Name="Normal"/>
        <VisualState Name="PointerOver">
          <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF1098DF"/>
          <Storyboard>
            <DoubleAnimation Storyboard.TargetName="Chrome"
                             Storyboard.TargetProperty="Opacity"
                             From="0.72"
                             To="1"
                             Duration="0:0:0.12"/>
          </Storyboard>
        </VisualState>
        <VisualState Name="Pressed">
          <Setter TargetName="Chrome" Property="Background" Value="#FFB9DDF5"/>
        </VisualState>
        <VisualState Name="Disabled">
          <Setter TargetName="Chrome" Property="Background" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="FocusStates">
        <VisualState Name="Focused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unfocused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
        </VisualState>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Hyperlink">
    <Setter Property="Foreground" Value="{StaticResource AccentFill}"/>
    <Setter Property="TextDecorations" Value="Underline"/>
  </Style>

  <Style TargetType="RepeatButton">
    <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
    <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8"/>
    <Setter Property="Foreground" Value="{StaticResource ControlForeground}"/>
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="RepeatButton">
    <ControlTemplate.VisualTree>
      <Grid>
        <Border x:Name="Chrome"
                Background="{StaticResource ControlBackground}"
                BorderBrush="{StaticResource ControlBorder}"
                BorderThickness="1"
                Padding="8">
          <ContentPresenter x:Name="ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
        </Border>
        <Border x:Name="FocusCue"
                BorderBrush="#00000000"
                BorderThickness="1"/>
      </Grid>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualStateGroup.Transitions>
          <VisualTransition GeneratedDuration="0:0:0.09"/>
        </VisualStateGroup.Transitions>
        <VisualState Name="Normal"/>
        <VisualState Name="PointerOver">
          <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Pressed">
          <Setter TargetName="Chrome" Property="Background" Value="#FFB9DDF5"/>
        </VisualState>
        <VisualState Name="Disabled">
          <Setter TargetName="Chrome" Property="Background" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="FocusStates">
        <VisualState Name="Focused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unfocused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
        </VisualState>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ToggleButton">
    <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
    <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8"/>
    <Setter Property="Foreground" Value="{StaticResource ControlForeground}"/>
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="ToggleButton">
    <ControlTemplate.VisualTree>
      <Grid>
        <Border x:Name="Chrome"
                Background="{StaticResource ControlBackground}"
                BorderBrush="{StaticResource ControlBorder}"
                BorderThickness="1"
                Padding="8">
          <ContentPresenter x:Name="ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
        </Border>
        <Border x:Name="FocusCue"
                BorderBrush="#00000000"
                BorderThickness="1"/>
      </Grid>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualStateGroup.Transitions>
          <VisualTransition GeneratedDuration="0:0:0.09"/>
        </VisualStateGroup.Transitions>
        <VisualState Name="Normal"/>
        <VisualState Name="PointerOver">
          <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Pressed">
          <Setter TargetName="Chrome" Property="Background" Value="#FFB9DDF5"/>
        </VisualState>
        <VisualState Name="Disabled">
          <Setter TargetName="Chrome" Property="Background" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="FocusStates">
        <VisualState Name="Focused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unfocused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="CheckStates">
        <VisualState Name="Checked"/>
        <VisualState Name="Unchecked"/>
        <VisualState Name="Indeterminate"/>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="CheckBox">
    <Setter Property="Foreground" Value="{StaticResource ControlForeground}"/>
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="CheckBox">
    <ControlTemplate.VisualTree>
      <Grid>
        <Border x:Name="Indicator"
                Width="20"
                Height="20"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Background="{StaticResource CheckBoxBackground}"
                BorderBrush="{StaticResource ControlBorder}"
                BorderThickness="1"/>
        <TextBlock x:Name="CheckGlyph"
                   Text=""
                   Margin="3,0,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Foreground="#FFFFFFFF"/>
        <Border x:Name="Chrome"
                Margin="26,0,0,0"
                Background="#00000000"
                BorderBrush="#00000000"
                BorderThickness="1">
          <ContentPresenter x:Name="ContentPresenter"
                            VerticalAlignment="Center"/>
        </Border>
        <Border x:Name="FocusCue"
                BorderBrush="#00000000"
                BorderThickness="1"/>
      </Grid>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualState Name="Normal"/>
        <VisualState Name="PointerOver">
          <Setter TargetName="Indicator" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Pressed">
          <Setter TargetName="Indicator" Property="BorderBrush" Value="#FFB9DDF5"/>
        </VisualState>
        <VisualState Name="Disabled">
          <Setter TargetName="Indicator" Property="BorderBrush" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="FocusStates">
        <VisualState Name="Focused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unfocused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="CheckStates">
        <VisualState Name="Checked">
          <Setter TargetName="Indicator" Property="Background" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unchecked"/>
        <VisualState Name="Indeterminate">
          <Setter TargetName="Indicator" Property="Background" Value="#FF5A9BD5"/>
        </VisualState>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="RadioButton">
    <Setter Property="Foreground" Value="{StaticResource ControlForeground}"/>
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="RadioButton">
    <ControlTemplate.VisualTree>
      <Grid>
        <Border x:Name="Indicator"
                Width="16"
                Height="16"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Background="{StaticResource ControlBackground}"
                BorderBrush="{StaticResource ControlBorder}"
                BorderThickness="1"/>
        <Border x:Name="Dot"
                Width="8"
                Height="8"
                Margin="4,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Background="#00000000"/>
        <Border x:Name="Chrome"
                Margin="22,0,0,0"
                Background="#00000000"
                BorderBrush="#00000000"
                BorderThickness="1">
          <ContentPresenter x:Name="ContentPresenter"
                            VerticalAlignment="Center"/>
        </Border>
        <Border x:Name="FocusCue"
                BorderBrush="#00000000"
                BorderThickness="1"/>
      </Grid>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualState Name="Normal"/>
        <VisualState Name="PointerOver">
          <Setter TargetName="Indicator" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Pressed">
          <Setter TargetName="Indicator" Property="BorderBrush" Value="#FFB9DDF5"/>
        </VisualState>
        <VisualState Name="Disabled">
          <Setter TargetName="Indicator" Property="BorderBrush" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="FocusStates">
        <VisualState Name="Focused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unfocused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="CheckStates">
        <VisualState Name="Checked">
          <Setter TargetName="Dot" Property="Background" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unchecked"/>
        <VisualState Name="Indeterminate">
          <Setter TargetName="Dot" Property="Background" Value="#FF5A9BD5"/>
        </VisualState>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ListBox">
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="ListBox">
    <ControlTemplate.VisualTree>
      <Border x:Name="Chrome"
              Background="{StaticResource ControlBackground}"
              BorderBrush="{StaticResource ControlBorder}"
              BorderThickness="1"
              Padding="2">
        <VirtualizingStackPanel x:Name="ItemsHost"
                                EstimatedItemExtent="24"
                                OverscanCount="2"/>
      </Border>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualState Name="Normal"/>
        <VisualState Name="Disabled">
          <Setter TargetName="Chrome" Property="Background" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ListBoxItem">
    <Setter Property="Template">
      <Setter.Value>
  <ControlTemplate TargetType="ListBoxItem">
    <ControlTemplate.VisualTree>
      <Grid>
        <Border x:Name="Chrome"
                Background="#00000000"
                BorderBrush="#00000000"
                BorderThickness="1"
                Padding="6"/>
        <Border x:Name="SelectionChrome"
                Background="#00000000"
                BorderBrush="#00000000"
                BorderThickness="0"/>
        <ContentPresenter x:Name="ContentPresenter"/>
        <Border x:Name="FocusCue"
                BorderBrush="#00000000"
                BorderThickness="1"/>
      </Grid>
    </ControlTemplate.VisualTree>
    <ControlTemplate.VisualStateGroups>
      <VisualStateGroup Name="CommonStates">
        <VisualState Name="Normal"/>
        <VisualState Name="PointerOver">
          <Setter TargetName="Chrome" Property="Background" Value="#332AA6E2"/>
        </VisualState>
        <VisualState Name="Disabled">
          <Setter TargetName="Chrome" Property="Background" Value="#FF6B7379"/>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup Name="SelectionStates">
        <VisualState Name="Selected">
          <Setter TargetName="SelectionChrome" Property="Background" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unselected"/>
      </VisualStateGroup>
      <VisualStateGroup Name="FocusStates">
        <VisualState Name="Focused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
        </VisualState>
        <VisualState Name="Unfocused">
          <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
        </VisualState>
      </VisualStateGroup>
    </ControlTemplate.VisualStateGroups>
  </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ItemsControl">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ItemsControl">
          <ControlTemplate.VisualTree>
            <ItemsPresenter x:Name="ItemsPresenter"/>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="TreeView">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TreeView">
          <ControlTemplate.VisualTree>
            <Border x:Name="Chrome"
                    Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="4">
              <StackPanel x:Name="ItemsHost"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="TreeViewItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TreeViewItem">
          <ControlTemplate.VisualTree>
            <StackPanel>
              <Grid ColumnDefinitionsText="18,*">
                <Border x:Name="HeaderChrome"
                        Grid.ColumnSpan="2"
                        Background="#00000000"
                        BorderBrush="#00000000"
                        BorderThickness="1"/>
                <TextBlock x:Name="ExpanderGlyph"
                           Grid.Column="0"
                           Margin="4,2,2,2"/>
                <TextBlock x:Name="HeaderText"
                           Grid.Column="1"
                           Margin="2"/>
              </Grid>
              <ItemsControl x:Name="ChildItems"
                            Margin="18,0,0,0"
                            Visibility="Collapsed"/>
            </StackPanel>
          </ControlTemplate.VisualTree>
          <ControlTemplate.VisualStateGroups>
            <VisualStateGroup Name="SelectionStates">
              <VisualState Name="Selected">
                <Setter TargetName="HeaderChrome"
                        Property="Background"
                        Value="#FF1098DF"/>
              </VisualState>
              <VisualState Name="Unselected"/>
            </VisualStateGroup>
          </ControlTemplate.VisualStateGroups>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Menu">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Menu">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="2">
              <StackPanel x:Name="ItemsHost"
                          Orientation="Horizontal"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ContextMenu">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ContextMenu">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="3">
              <StackPanel x:Name="ItemsHost"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="MenuItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="MenuItem">
          <ControlTemplate.VisualTree>
            <StackPanel>
              <Grid ColumnDefinitionsText="18,*,80,18">
                <Border x:Name="HeaderChrome"
                        Grid.ColumnSpan="4"
                        Background="#00000000"
                        BorderBrush="#00000000"
                        BorderThickness="1"/>
                <TextBlock x:Name="CheckGlyph"
                           Grid.Column="0"
                           Margin="4,3,2,3"/>
                <TextBlock x:Name="HeaderText"
                           Grid.Column="1"
                           Margin="5,3"/>
                <TextBlock x:Name="GestureText"
                           Grid.Column="2"
                           Margin="8,3"/>
                <TextBlock x:Name="ExpanderGlyph"
                           Grid.Column="3"
                           Margin="3"/>
              </Grid>
              <Popup x:Name="SubmenuPopup"
                     Placement="Right"
                     StaysOpen="True">
                <Border Background="{StaticResource ControlBackground}"
                        BorderBrush="{StaticResource ControlBorder}"
                        BorderThickness="1"
                        Padding="2">
                  <ItemsControl x:Name="ChildItems"
                                Visibility="Collapsed"/>
                </Border>
              </Popup>
            </StackPanel>
          </ControlTemplate.VisualTree>
          <ControlTemplate.VisualStateGroups>
            <VisualStateGroup Name="SelectionStates">
              <VisualState Name="Selected">
                <Setter TargetName="HeaderChrome"
                        Property="Background"
                        Value="#332AA6E2"/>
              </VisualState>
              <VisualState Name="Unselected"/>
            </VisualStateGroup>
          </ControlTemplate.VisualStateGroups>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ListView">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListView">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="2">
              <StackPanel>
                <Border Background="{StaticResource ControlHoverBackground}"
                        BorderBrush="{StaticResource ControlBorder}"
                        BorderThickness="0"
                        Padding="6,4">
                  <TextBlock x:Name="ColumnHeaders"/>
                </Border>
                <VirtualizingStackPanel x:Name="ItemsHost"
                                        EstimatedItemExtent="28"
                                        OverscanCount="2"/>
              </StackPanel>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ListViewItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListViewItem">
          <ControlTemplate.VisualTree>
            <Grid>
              <Border x:Name="Chrome"
                      Background="#00000000"
                      BorderBrush="#00000000"
                      BorderThickness="1"
                      Padding="6"/>
              <ContentPresenter x:Name="ContentPresenter"/>
              <Border x:Name="FocusCue"
                      BorderBrush="#00000000"
                      BorderThickness="1"/>
            </Grid>
          </ControlTemplate.VisualTree>
          <ControlTemplate.VisualStateGroups>
            <VisualStateGroup Name="SelectionStates">
              <VisualState Name="Selected">
                <Setter TargetName="Chrome"
                        Property="Background"
                        Value="#FF1098DF"/>
              </VisualState>
              <VisualState Name="Unselected"/>
            </VisualStateGroup>
          </ControlTemplate.VisualStateGroups>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ToolBar">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToolBar">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="3">
              <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="HeaderText"
                           Margin="4"/>
                <StackPanel x:Name="ItemsHost"
                            Orientation="Horizontal"/>
                <TextBlock x:Name="OverflowGlyph"
                           Margin="6,4"/>
              </StackPanel>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="StatusBar">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="StatusBar">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="3">
              <Grid ColumnDefinitionsText="*,Auto">
                <StackPanel x:Name="ItemsHost"
                            Grid.Column="0"
                            Orientation="Horizontal"/>
                <TextBlock Grid.Column="1"
                           Text="///"
                           Margin="5,2"/>
              </Grid>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="StatusBarItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="StatusBarItem">
          <ControlTemplate.VisualTree>
            <Border Padding="5,2">
              <ContentPresenter x:Name="ContentPresenter"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Separator">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Separator">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBorder}"
                    Width="1"
                    Height="20"
                    Margin="4,2"/>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ToolTip">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToolTip">
          <ControlTemplate.VisualTree>
            <Border Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="8">
              <ContentPresenter x:Name="ContentPresenter"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="MinHeight"
            Value="38"/>
    <Setter Property="FontSize"
            Value="15"/>
    <Setter Property="Background"
            Value="{StaticResource TextBoxBackground}"/>
    <Setter Property="Foreground"
            Value="{StaticResource TextBoxForeground}"/>
    <Setter Property="PlaceholderForeground"
            Value="{StaticResource TextBoxPlaceholder}"/>
    <Setter Property="BorderBrush"
            Value="{StaticResource TextBoxBorder}"/>
    <Setter Property="BorderThickness"
            Value="1"/>
    <Setter Property="Padding"
            Value="6,7"/>
    <Setter Property="SelectionBrush"
            Value="{StaticResource TextBoxSelection}"/>
    <Setter Property="SelectionOpacity"
            Value="0.25"/>
  </Style>

  <Style TargetType="ComboBox">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <ControlTemplate.VisualTree>
            <Grid>
              <Border x:Name="Chrome"
                      Background="{StaticResource ControlBackground}"
                      BorderBrush="{StaticResource ControlBorder}"
                      BorderThickness="1"
                      Padding="8"/>
              <TextBlock x:Name="SelectionBox"
                         Margin="8,0,28,0"
                         VerticalAlignment="Center"/>
              <TextBox x:Name="PART_EditableTextBox"
                       Visibility="Collapsed"
                       Margin="4,2,28,2"
                       VerticalAlignment="Stretch"/>
              <TextBlock Text="v"
                         Margin="0,0,10,0"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Center"/>
              <Border x:Name="FocusCue"
                      BorderBrush="#00000000"
                      BorderThickness="1"/>
              <Popup x:Name="PART_Popup"
                     Placement="Bottom"
                     StaysOpen="False"
                     MatchPlacementTargetWidth="True">
                <Border x:Name="DropDownBorder"
                        Background="{StaticResource ControlBackground}"
                        BorderBrush="{StaticResource ControlBorder}"
                        BorderThickness="1"
                        Padding="2">
                  <VirtualizingStackPanel x:Name="ItemsHost"
                                          EstimatedItemExtent="28"
                                          OverscanCount="2"/>
                </Border>
              </Popup>
            </Grid>
          </ControlTemplate.VisualTree>
          <ControlTemplate.VisualStateGroups>
            <VisualStateGroup Name="CommonStates">
              <VisualState Name="Normal"/>
              <VisualState Name="PointerOver">
                <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF1098DF"/>
              </VisualState>
              <VisualState Name="Disabled">
                <Setter TargetName="Chrome" Property="Background" Value="#FF6B7379"/>
              </VisualState>
            </VisualStateGroup>
            <VisualStateGroup Name="FocusStates">
              <VisualState Name="Focused">
                <Setter TargetName="FocusCue" Property="BorderBrush" Value="#FF1098DF"/>
              </VisualState>
              <VisualState Name="Unfocused">
                <Setter TargetName="FocusCue" Property="BorderBrush" Value="#00000000"/>
              </VisualState>
            </VisualStateGroup>
          </ControlTemplate.VisualStateGroups>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <ControlTemplate.VisualTree>
            <Grid>
              <Border x:Name="Chrome"
                      Background="#00000000"
                      BorderBrush="#00000000"
                      BorderThickness="1"
                      Padding="6"/>
              <Border x:Name="SelectionChrome"
                      Background="#00000000"
                      BorderBrush="#00000000"
                      BorderThickness="0"/>
              <ContentPresenter x:Name="ContentPresenter"/>
            </Grid>
          </ControlTemplate.VisualTree>
          <ControlTemplate.VisualStateGroups>
            <VisualStateGroup Name="SelectionStates">
              <VisualState Name="Selected">
                <Setter TargetName="SelectionChrome" Property="Background" Value="#FF1098DF"/>
              </VisualState>
              <VisualState Name="Unselected"/>
            </VisualStateGroup>
          </ControlTemplate.VisualStateGroups>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="PasswordBox">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="PasswordBox">
          <ControlTemplate.VisualTree>
            <Border x:Name="Chrome"
                    Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="6"/>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Slider">
    <Setter Property="Foreground"
            Value="#FF1098DF"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <ControlTemplate.VisualTree>
            <Grid/>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ProgressBar">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ProgressBar">
          <ControlTemplate.VisualTree>
            <Grid>
              <Border x:Name="TrackChrome"
                      Background="{StaticResource DisabledBackground}"
                      BorderBrush="{StaticResource ControlBorder}"
                      BorderThickness="1"/>
              <Border x:Name="Indicator"
                      Background="#FF1098DF"
                      BorderBrush="#00000000"
                      BorderThickness="0"/>
            </Grid>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="GroupBox">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="GroupBox">
          <ControlTemplate.VisualTree>
            <Border x:Name="Chrome"
                    Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="8">
              <ContentPresenter x:Name="ContentPresenter"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="TabItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabItem">
          <ControlTemplate.VisualTree>
            <Border x:Name="Chrome"
                    Background="{StaticResource ControlBackground}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Padding="6">
              <ContentPresenter x:Name="ContentPresenter"/>
            </Border>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="TabControl">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabControl">
          <ControlTemplate.VisualTree>
            <Grid RowDefinitionsText="Auto,*">
              <TabPanel x:Name="ItemsHost"/>
              <Border x:Name="ContentChrome"
                      Grid.Row="1"
                      Background="{StaticResource ControlBackground}"
                      BorderBrush="{StaticResource ControlBorder}"
                      BorderThickness="1"
                      Padding="8">
                <ContentPresenter x:Name="PART_SelectedContentHost"/>
              </Border>
            </Grid>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ScrollViewer">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollViewer">
          <ControlTemplate.VisualTree>
            <Grid RowDefinitionsText="*,Auto" ColumnDefinitionsText="*,Auto">
              <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"/>
              <ScrollBar x:Name="PART_VerticalScrollBar"
                         Grid.Column="1"
                         Orientation="Vertical"/>
              <ScrollBar x:Name="PART_HorizontalScrollBar"
                         Grid.Row="1"
                         Orientation="Horizontal"/>
            </Grid>
          </ControlTemplate.VisualTree>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
