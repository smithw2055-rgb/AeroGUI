#pragma once

// Canonical default application-framework entry point. Launcher is currently
// a source-compatible alias of ApplicationHost while implementation files are
// migrated in later phases.
#include <Aero/App/ApplicationHost.hpp>
