#pragma once

// Visual-state types and VisualStateManager are currently implemented with the
// ControlTemplate runtime. Keep the public semantic entry stable while the
// declarations are split into their final ownership unit.
#include <Aero/Controls/Templates.hpp>
