#pragma once

#include <Aero/Presentation/Layout.hpp>

namespace Aero {

using Point = Base::Point;
using Size = Base::Size;
using Rect = Base::Rect;
using Thickness = Base::Thickness;
using CornerRadius = Base::CornerRadius;

using HorizontalAlignment =
    Presentation::HorizontalAlignment;
using VerticalAlignment =
    Presentation::VerticalAlignment;
using Visibility = Presentation::Visibility;
using Length = Presentation::Length;

} // namespace Aero
