#include <Aero/Core/MetadataDsl.hpp>

// Typed metadata registration is header-defined so external controls receive
// the same compile-time checks as built-in controls. This translation unit
// intentionally owns no legacy registration builder or compatibility macros.
